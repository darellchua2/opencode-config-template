{
  "$comment": "Provider pack: Next.js devtools MCP server (runtime diagnosis for Next.js 16+ projects). Deep-merged by deploy/merge-packs.mjs when the user passes --enable-pack nextjs. Sets a root permissions rule {action: \"next-devtools*\", effect: \"allow\"} so the rule-merge flips the source-config deny in place.",
  "mcp": {
    "servers": {
      "next-devtools": { "disabled": false }
    }
  },
  "permissions": [
    { "action": "next-devtools*", "resource": "*", "effect": "allow" }
  ]
}
