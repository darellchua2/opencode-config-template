{
  "$comment": "Non-frontmatter dependency edges for deploy/init.mjs. The PRIMARY dependency source is agent frontmatter (permission.skill = requiresSkills, permission.task = delegatesTo). This map covers only the skill->MCP edge that frontmatter cannot express: when a skill is installed, the listed MCP server(s) should also be enabled. Values MUST match mcp.<key> in opencode_app/opencode.json.",
  "impliesMcp": {
    "nextjs-devtools-mcp-skill": ["next-devtools"],
    "markitdown-mcp-skill": ["markitdown"],
    "codegraph-setup-skill": ["codegraph"]
  }
}
