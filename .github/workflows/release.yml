name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      create_release:
        description: 'Create a release manually'
        required: true
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Lint validation
        run: |
          echo "Validating deploy/config.json..."
          jq . deploy/config.json
          echo "config.json is valid"

          echo "Validating deploy/setup.sh syntax..."
          bash -n deploy/setup.sh
          echo "setup.sh syntax is valid"
        continue-on-error: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Install semantic-release plugins
        run: |
          npm install --no-save \
            semantic-release \
            @semantic-release/commit-analyzer \
            @semantic-release/release-notes-generator \
            @semantic-release/changelog \
            @semantic-release/exec \
            @semantic-release/git \
            @semantic-release/github

      - name: Create Release with CHANGELOG
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🚀 Running semantic-release..."
          echo "📝 This will:"
          echo "   1. Analyze commits to determine version bump"
          echo "   2. Update VERSION file with new version"
          echo "   3. Update CHANGELOG.md with release notes"
          echo "   4. Create git tag (vX.Y.Z)"
          echo "   5. Create GitHub release"
          echo ""
          npx semantic-release

      - name: Upload CHANGELOG as workflow artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: CHANGELOG
          path: CHANGELOG.md
          retention-days: 30
